import typing
from typing import Literal, Tuple

# Define OpsType
OpsType = Literal['make mask', 'clip only', 'both']

# Define Return Periods
RETURNPERIOD = ['0.2%', '1%', '2%', '5%', '10%', '25%']

RETURNFILESTRING = ['0_2pct', '01pct', '02pct', '05pct', '10pct', '25pct']

PERC = [0.002, 0.01, 0.02, 0.05, 0.10, 0.25]
